<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- Strip permissions not needed by FlockFree for Play Store build -->
    <uses-permission android:name="android.permission.CAMERA" tools:node="remove"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.STORAGE" tools:node="remove"/>

    <!-- Remove features not needed by FlockFree -->
    <uses-feature android:name="android.hardware.camera" tools:node="remove"/>
    <uses-feature android:name="android.hardware.camera.autofocus" tools:node="remove"/>
    <uses-feature android:name="android.hardware.microphone" tools:node="remove"/>

    <application
        android:icon="@mipmap/icon_free"
		android:label="@string/app_name"
        android:requestLegacyExternalStorage="false"
        tools:replace="android:icon, android:label, android:requestLegacyExternalStorage">

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="fb2057524931189530"/>
        <activity
            android:name="net.osmand.plus.activities.MapActivity"
            android:theme="@style/FirstSplashScreenFree"
            tools:replace="android:theme"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            tools:replace="android:authorities"
            android:authorities="net.osmand.fileprovider"/>
    </application>
</manifest> 